const map = {
    width: 2000,
    height: 1600,
    bgColor: '#eee',
    minScale: 0.5,
    maxScale: 2,
    bias: {
        x: 500,
        y: 400
    },
    initScale: 0.6,
}

export default map